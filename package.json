{
  "name": "your-story",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first memory bank and interview practice app",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/app.js && node --check dist/core.js && node --check server.mjs && node --check ai.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
